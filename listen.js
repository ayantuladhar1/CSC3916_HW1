//Ayan Tuladhar
//HW1
//The program below was compiled from lecture.

var http = require("http");
var server = http.createServer();
server.on("request", (req, res) =>
{
    // will write our action hers.
});